# Netlify redirects removed — project now uses Vercel

# Vercel handles redirects/rewrites and headers via `vercel.json` at the project root.

# Remove this file or keep as documentation; it will not be used by Vercel.
