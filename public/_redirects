# Netlify Redirects Configuration
# This file ensures proper routing and SEO optimization

# Main redirect - redirect non-www to www (or vice versa)
# If using miracle-portfolio.netlify.app, this is automatic

# Redirect all HTTP to HTTPS (Netlify handles this automatically)
# Status codes: 301 (permanent), 302 (temporary)

# Redirect trailing slashes for consistency
[[redirects]]
from = "/*"
to = "/index.html"
status = 200

# Security headers are set via netlify.toml
